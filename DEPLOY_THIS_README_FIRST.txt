AXON PERFORMANCE WAIVER KIOSK — EASY NETLIFY DEPLOY

FASTEST PATH — NO NETLIFY AI AGENT RUNS
1. Unzip this package.
2. Drag the folder named AXON_KIOSK_DROP_THIS_FOLDER into Netlify Drop or your existing site's Deploys drop zone.
3. Do NOT run a Netlify Agent. Do NOT ask Netlify AI to rebuild it.
4. There is no npm install and no build command.
5. Publish directory is the root folder, already set in netlify.toml.

ADMIN ACCESS
Admin code: 1307! or 1307 fallback.

EMAIL NOTIFICATIONS — OPTIONAL AFTER FIRST DEPLOY
If you want signed-contract notification emails, set these in Netlify environment variables:
RESEND_API_KEY=re_xxxxxxxxx
FROM_EMAIL=Axon Performance <contracts@yourdomain.com>
ALLOWED_NOTIFY_EMAILS=your@email.com,manager@email.com

After adding variables, redeploy this same folder once.

WHAT TO UPLOAD
Upload or drag the FOLDER, not the files one by one.
The folder must contain index.html, app.js, styles.css, assets, netlify.toml, and netlify/functions.
